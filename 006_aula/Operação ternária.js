// (condição) ? Valor para Verdadeiro : Valor para Falso

const usuario = 2222;
const nivelUsuario = usuario >= 1000 ? 'Usuário VIP' : 'Usuário Normal';
console.log(nivelUsuario);



/* 
if (usuario >= 1000) {
  console.log('Usuário VIP');
} else {
  console.log('Usuário Normal');
}
*/

